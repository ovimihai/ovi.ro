source "https://rubygems.org"

# Mirrors GitHub Pages' build environment (Jekyll 3.9.x).
gem "github-pages", "~> 228", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-paginate"
  gem "jekyll-feed"
end

# Keep native deps compatible with the system Ruby used for local builds.
gem "ffi", "< 1.17"
